rules:
  template-injection:
    ignore:
      - v-release.yml
  excessive-permissions:
    ignore:
      - v-release.yml
  undocumented-permissions:
    ignore:
      - v-release.yml
  anonymous-definition:
    ignore:
      - v-release.yml
  concurrency-limits:
    ignore:
      - v-release.yml
  unpinned-images:
    ignore:
      - v-release.yml
