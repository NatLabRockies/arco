
model Dispatch {
  control p {
    index g
    index t
  }

  constraint ramp {
    index g
    index tt { in t }
    if { tt > 1 }
    expression {
      p[g,tt] - p[g,tt-1] <= 10
    }
  }

  minimize Obj {
    sum(p[g,t] for g in g for t in t)
  }
}

scenario Base {
  use Dispatch
}
