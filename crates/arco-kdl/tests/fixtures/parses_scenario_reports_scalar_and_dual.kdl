
model "Dispatch" {
  set "assets"
  control "dispatch" index="assets"
  constraint "balance" {
    dispatch[a] <= 100
  }
  minimize "Cost" {
    dispatch[a]
  }
}

scenario "Base" {
  use "Dispatch"
  data "demand" source="data/demand.csv"
  report FuelCost
  report dual balance
}
